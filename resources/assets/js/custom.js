const Swal = require('sweetalert2');

const delete_action = (e) => {
    alert();
};

$('#datepicker').datepicker({
    startDate: "today",
    todayBtn: true,
    clearBtn: true,
    language: "es"
});